apiVersion: v1
kind: Service
metadata:
  name: nginx-php-svc
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      protocol: TCP
  selector:
    app: php-lbl
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: php-lbl
  template:
    metadata:
      labels:
        app: php-lbl
    spec:
      containers:
        - image: php_img
          imagePullPolicy: Never
          name: phpmyadmin
          ports:
            - containerPort: 5000
